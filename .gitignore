*.iml
.gradle
/local.properties
/.idea/
/.ua/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
.cursor
.history
.kotlin

# --- Personal / AI-agent workspace (not for the public fork) ---
/.claude/
/.superpowers/
/docs/superpowers/
SESSION-STATE.md
**/CLAUDE.md

# Sibling clones of upstream projects used as references during development
/termux-api/

# Sensitive build inputs (signed-in service account / Firebase config)
app/google-services.json
# Release artifacts staged for GitHub release uploads — debug-signed APKs are
# regenerated each release; no need to track them in git.
/release-artifacts/

# Release signing keystore — never commit
/rikkahub-release.jks

# Local planning doc — not for the remote
/enhance.md

# Separate companion repo - cloned locally, not part of this repo
/agent-keyboard/

# Generated baseline profiles (baseline profiling dropped in this fork)
/app/src/release/generated/

# Planning docs — not for the remote
/plans/
app/src/main/jni/litert/litert_cc_sdk/

# OpenCode plugin install artifacts — not project code
.opencode/

# Proactive agent session state — local memory, not for the remote
/MEMORY.md
/SESSION-STATE.md
/memory/

# Sensitive developer backup archives — may contain API keys; keep out of the repo
/var/tmp/rikkahub-local-backups/
.rikkahub-backup/
